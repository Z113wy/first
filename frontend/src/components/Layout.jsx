import React from "react"; import { Layout as AntLayout, Menu, Button, Avatar, Dropdown } from "antd"; import { Outlet, useNavigate, useLocation, Navigate } from "react-router-dom"; import { useAuth } from "../contexts/AuthContext"; import { UserOutlined, LogoutOutlined, DashboardOutlined, FormOutlined, BarChartOutlined, FileTextOutlined } from "@ant-design/icons"; const { Header, Sider, Content } = AntLayout; const Layout = () => { const { user, logout } = useAuth(); const navigate = useNavigate(); const location = useLocation(); const menuItems = [ { key: "/dashboard", icon: <DashboardOutlined />, label: "仪表板", }, { key: "/create", icon: <FormOutlined />, label: "创建问卷", }, { key: "/surveys", icon: <FileTextOutlined />, label: "我的问卷", }, { key: "/my-answers", icon: <BarChartOutlined />, label: "我的回答", }, ]; const userMenuItems = [ { key: "profile", icon: <UserOutlined />, label: "个人资料", }, { key: "logout", icon: <LogoutOutlined />, label: "退出登录", onClick: () => { logout(); navigate("/login"); }, }, ]; if (!user) { return <Navigate to="/login" replace />; } return (<AntLayout style={{ minHeight: "100vh" }}><Header style={{ display: "flex", alignItems: "center", justifyContent: "space-between", background: "#fff", padding: "0 24px", }}><div style={{ fontSize: "20px", fontWeight: "bold", color: "#1890ff" }}>问卷系统</div><Dropdown menu={{ items: userMenuItems }} placement="bottomRight"><Button type="text" style={{ height: "auto", padding: "4px 8px" }}><Avatar size="small" icon={<UserOutlined />} />{user.username}</Button></Dropdown></Header><AntLayout><Sider width={200} style={{ background: "#fff" }}><Menu mode="inline" selectedKeys={[location.pathname]} style={{ height: "100%", borderRight: 0 }} items={menuItems} onClick={({ key }) => navigate(key)} /></Sider><Content style={{ margin: "24px", padding: "24px", background: "#fff", minHeight: 280, }}><Outlet /></Content></AntLayout></AntLayout>); }; export default Layout;
